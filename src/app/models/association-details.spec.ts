import { AssociationDetails } from './association-details';

describe('AssociationDetails', () => {
  it('should create an instance', () => {
    expect(new AssociationDetails()).toBeTruthy();
  });
});
